"use strict";

const width = Number(5);
const height = Number(7.1);
const area = width * height;

const logic = Boolean(true == false);
const str = String("hello");
const megastr = String(`${width} * ${height} = ${area}`);
const a = Object();

a.h = 1;
console.log(a);
